# AI-Payment-Routing-sys
End-to-end ML system for intelligent payment routing using synthetic transaction data. Features rolling-window stats, time-aware training, stacked ensemble (RF + XGBoost + Logistic Regression), explainable AI via SHAP, and simulated online retraining for adaptive, data-driven gateway selection.
